// The runtime has one display camera and one model-query camera.
// Keep these values in one module so an old asset cannot silently change them.
export const FRONTEND_RENDER_FOV_Y_DEG = 60;
export const MODEL_INPUT_FOV_Y_DEG = 66;

